var superHeroes = []; //superHeroes is an array of strings
var heroPowers = []; //heroPowers is an array of numbers
var heroPower = []; //heroPower is also an array of numbers, using a different syntax
superHeroes.push('Batman');
heroPowers.push(100);
var allUsers = []; //allUsers is an array of User objects
allUsers.push({ name: 'Ian', isActive: true }); //pushing a User object into the allUsers array following the structure defined by the User type
